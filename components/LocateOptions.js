import { View, Text } from "react-native";
import React from "react";

const LocateOptions = () => {
  return (
    <View>
      <Text>LocateOptions</Text>
    </View>
  );
};

export default LocateOptions;
